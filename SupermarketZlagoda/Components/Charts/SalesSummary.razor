@page "/charts"
@using SupermarketZlagoda.Data.Model
<h3>SalesSummary</h3>

<FluentToolbar>
    <FluentSelect TOption="Employee"
                  Label="Select an employee"
                  Items="@_employees"
                  Id="empl-listbox"
                  Width="200px"
                  Height="250px"
                  OptionValue="@(p => p.Id.ToString())"
                  OptionText="@(p => p.Surname + p.Name)"
                  @bind-Value="@SelectedEmployeeId"
                  @bind-SelectedOption="@SelectedEmployee"/>
    <FluentSelect TOption="Product"
                  Label="Select an product"
                  Items="@_products"
                  Id="product-listbox"
                  Width="200px"
                  Height="250px"
                  OptionValue="@(p => p.Id.ToString())"
                  OptionText="@(p => p.Name)"
                  @bind-Value="@SelectedProductId"
                  @bind-SelectedOption="@SelectedStoreProduct"/>
    <FluentDatePicker Label="Date From:" @bind-Value="@DateTimeFrom"/>
    <FluentDatePicker Label="Date To:" @bind-Value="@DateTimeTo"/>
</FluentToolbar>
<BarChart @ref="_barChart" Height="800" Width="1000" Class="mb-4"/>


@code {


}